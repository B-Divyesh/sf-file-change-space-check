# Landing-page copy audit

Audited on 5 September 2026 from `site/index.html`. Words are tokens containing
letters or numbers. No sentence exceeds 22 words. No banned word appears.

## Sentences

| Words | Sentence |
| ---: | --- |
| 17 | For people moving large folders who need a space and conflict estimate before a long copy starts. |
| 7 | See a finished plan in one click. |
| 7 | Files are measured before any copy begins. |
| 8 | The bundled demo creates a temporary media archive. |
| 9 | It shows one conflict and a sparse disk image. |
| 2 | Read-only plan. |
| 8 | No source or destination files were changed. |
| 9 | The recording is a transcript from the packaged binary. |
| 10 | Run `fcsc --demo` to create a fresh sandbox. |
| 12 | Read file names, sizes, allocated blocks, entry types, and destination free space. |
| 10 | Choose overwrite, skip, or keep-both for paths that already exist. |
| 12 | Compare safe headroom with free space, then print a deterministic JSON manifest. |
| 6 | Rust 1.85 or newer is required. |
| 8 | The direct download is a Linux x86-64 binary. |
| 11 | No transfer: the JSON manifest does not copy or delete files. |
| 13 | No permission promise: readable metadata does not prove a later copy can write. |
| 12 | Sparse range: allocation depends on the filesystem and the copy tool. |
| 11 | Local run: the CLI works without accounts or network access. |
| 9 | Check space and conflicts before a local file copy. |

## Headings, actions, facts, and labels

All fragments are 9 words or fewer. The first screen reads aloud as: “Check
space before you copy files. For people moving large folders who need a space
and conflict estimate before a long copy starts. Try it with sample data.”

- Headline: `Check space before you copy files` — 7 words.
- Primary action: `Try it with sample data` — 5 words.
- Facts: `Reads file metadata, not file contents`; `Changes no source or
  destination files`; `Free to use, with no telemetry` — 6 words each.
- Section headings: `See the sample plan before installing`; `Plan one copy in
  three steps`; `Install the CLI from source`; `Know what the plan does not
  cover` — 5 to 7 words.
- Process labels: `Scan metadata`; `Apply policy`; `Check space`; `Write JSON` —
  2 words each.

## Terminology

| Concept | One term used |
| --- | --- |
| planned file operation | copy |
| generated action data | manifest |
| space needed before reclaim | headroom |
| existing destination path | conflict |
| isolated sample | demo |
| filesystem information | metadata |
| duplicate-name policy | keep-both |
